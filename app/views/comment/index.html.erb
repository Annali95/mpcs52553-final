<p><%= link_to "Add a article", "/articles/new", class: 'button' %></p>

  <table>
    <thead>
      <tr>
        <th></th>
        <th></th>
        <th>title</th>
      </tr>
    </thead>
    <tbody>

      <% @article.each do |article| %>

        <tr>
            <td><%= link_to "Toast", "/articles/#{article.id}", method: 'delete' %>
            <td><%= link_to "Edit", "/articles/#{article.id}/edit" %>
            <td><%= link_to "show", "/articles/#{article.id}" %>

            <td><%= article.title %></td>
        </tr>

      <% end %>
    </tbody>
  </table>
