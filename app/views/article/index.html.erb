<p><%= link_to "Add a article", "/article/new", class: 'button' %></p>

  <table class="table table-hover">
    <thead>
      <tr>
        <th></th>
        <th></th>
        <th>title</th>
      </tr>
    </thead>
    <tbody>

      <% @article.each do |article| %>

        <tr>
            <td><%= link_to "Toast", "/article/#{article.id}", method: 'delete', data: { confirm: 'Are you sure?' } %>
            <td><%= link_to article.title, "/article/#{article.id}" %></td>
            <td><%= article.content %></td>
        </tr>

      <% end %>
    </tbody>
  </table>
