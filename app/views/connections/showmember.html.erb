<% if @connection.present? %>
  <p><%= link_to "Add a member to the group", "/connections/#{params["id"]}", class: 'button' %></p>

    <table class="table table-hover">
      <thead>
        <tr>
          <th></th>
          <th></th>
          <th>title</th>
        </tr>
      </thead>
      <tbody>
        <% @connection.each do |connection| %>
          <tr>
              <td><%= link_to "Toast", "/connections/#{connection.group_id}/#{connection.user_id}", method: 'delete' %>
              <td><%= connection.user.name %></td>
              <td><%= connection.detail %></td>
          </tr>

        <% end %>
      </tbody>
    </table>
<%else%>  
<p>Nothing!</p>
<%end%>
