<!-- <p><%= link_to "Add to a Gruop", "/connection/new", class: 'button' %></p> -->




<% if @connection.present? %>
    <table class="table table-hover">
      <thead>
        <tr>
          <th></th>
          <th></th>
          <th>title</th>
        </tr>
      </thead>
      <tbody>

        <% @connection.each do |connection| %>
          <tr>
              <td><%= link_to "Drop off", "/connection/#{connection.id}", method: 'delete' %>

              <td><%= connection.group.name %></td>
              <td><%= connection.group.detail %></td>
              <td><%= link_to "group article", "/grouparticle/#{connection.group_id}" %>
          </tr>

        <% end %>
      </tbody>
    </table>
<%else%>  
<p>Nothing!</p>
<%end%>
